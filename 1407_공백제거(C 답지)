#include<stdio.h>
 
 
int main(){
    
    char buf[101];
    
    gets(buf);
    
    for (int i = 0; buf[i] != '\0'; i++) {
        if (buf[i] != ' '){
            printf("%c", buf[i]);
        }
    }
    
    return 0;
  
    
 
}
